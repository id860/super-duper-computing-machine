import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('compact header is loaded as the final visual layer',async()=>{const html=await readFile(new URL('../public/index.html',import.meta.url),'utf8');assert.ok(html.indexOf('topbar-compact.css')>html.indexOf('regression-fixes.css'));assert.match(html,/theme-color" content="#111827"/);});
test('compact header keeps mobile world identity and constrained controls',async()=>{const css=await readFile(new URL('../public/topbar-compact.css',import.meta.url),'utf8');assert.match(css,/#topbar[\s\S]*height:46px/);assert.match(css,/@media \(max-width:860px\)/);assert.match(css,/#worldName \{ display:block !important/);assert.match(css,/max-width:104px/);assert.match(css,/conic-gradient/);});
