import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('official progression is isolated',async()=>{let s=await readFile(new URL('../server.mjs',import.meta.url),'utf8');assert.match(s,/w\.type==='official'/);assert.match(s,/u\.officialPixels\+\+/);assert.match(s,/u\.communityPixels\+\+/)});test('no GitHub tokens',async()=>{for(const f of ['../server.mjs','../public/index.html'])assert.doesNotMatch(await readFile(new URL(f,import.meta.url),'utf8'),/ghp_[A-Za-z0-9]{20,}/)})